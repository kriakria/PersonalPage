export const getStaticPaths = async () => {
  // fetch data for each individual post and return an object with the format as below
  // return {
  //     paths: { includes an array of each object(posts in this case) }
  // }
};

export const getStaticProps = async () => {};
